<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Brukestøtte</title>
    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('_nav', { user: user }) %>

    <h1>Dine Guider</h1>

    <ul>
        <% if (guides.length > 0) { %>
            <% guides.forEach(guide => { %>
                <li>
                    <%= guide.title %> 
                    - <a href="/guide/<%= guide._id %>">Vis</a>
                    - <a href="/guide/<%= guide._id %>/edit">Rediger</a>
                    - <form action="/guide/<%= guide._id %>/delete" method="POST" style="display:inline;">
                        <button type="submit" onclick="return confirm('Er du sikker på at du vil slette denne guiden?')">Slett</button>
                    </form>
                </li>
            <% }) %>
        <% } else { %>
            <p>Du har ingen guider.</p>
        <% } %>
    </ul>

    <a href="/newguide">Lag en ny guide</a>
</body>
</html>
